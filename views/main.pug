<!DOCTYPE html>
html
    head
        meta(charset="UTF-8")
        title= title
        link(rel='stylesheet', href='/main.css')
    body        
        #page_wrapper
            #main_header
                #hgroup
                    h1.master_title
                        a(href='/') nodejs team project!
                    h2.master_description 2WDJ - 7조
            #main_navigation
                .pull_left
                    ul.outer_menu
                        l1.outer_menu_item
                            a(href="/menu1") 조원소개
                        l1.outer_menu_item
                            a(href="/menu2") 현지학기제
                        l1.outer_menu_item
                            a(href="/menu3") 질의응답
                .pull_right
                    .login_bar
                        if !user
                            a.input_login_button(href="/login") 로그인
                        else
                            - var name = user.name
                            p.self_profile_text #{name}님 어서오세요
                            a.input_logout_button(href="/auth/logout") 로그아웃
            block content

            #main_footer
                a(href="https://www.yju.ac.kr/kr/index.do") 영진전문대학교

    
                            