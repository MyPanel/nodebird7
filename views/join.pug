<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        title= title
        link(rel='stylesheet', href='/main.css')
    body
        .container
            .login_wrap
                form(action="/auth/join", method="post")
                    .input-group
                        label(for='userId') ID
                        input#email(type='text' name='userId' required autofocus)
                    .input-group
                        label(for='name') 이름
                        input#email(type='text' name='name' required)
                    .input-group
                        label(for='password') 비밀번호
                        input#password(type='password' name='password' required)
                    .error_message= joinError
                    button#login.btn(type='submit') 회원가입
        
